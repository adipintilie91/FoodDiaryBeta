@model IEnumerable<FoodDiaryBeta.Models.MealTypeModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IDMealType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MealTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IDMealType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MealTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.IDMealType}) |
            @Html.ActionLink("Details", "Details", new { id = item.IDMealType }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.IDMealType })
        </td>
    </tr>
}

</table>
